import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

// Using the render function for our entry point at the App component
ReactDOM.render(<App />, document.getElementById("root"));
